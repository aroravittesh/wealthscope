# AI Model Service (Python)

This service is copied from your provided Python AI files and kept separate from the existing `ai-service`.

## Included in repo

- `main.py`
- `predict.py`
- `requirements.txt`

## Model artifact files (required, not committed here)

Place these files in this same folder before running:

- `final_model.pth`
- `scaler.pkl`

`__pycache__` is auto-generated by Python and should not be committed.

## Backend integration

Backend endpoint:

- `POST /api/ai/recommend`

Backend forwards requests to:

- `${AI_SERVICE_URL}/recommend` (default `http://localhost:8000`)
